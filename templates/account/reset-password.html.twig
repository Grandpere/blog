{% extends 'base.html.twig' %}

{% block title %}
    Reset password!
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="post">
        <h1>Reset password </h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Reset') }}</button>
        {{ form_end(form) }}
    </div
{% endblock %}