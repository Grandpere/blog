{% extends 'base.html.twig' %}

{% block title %}
    Forgotten password!
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="post">
        <h1>Forgotten password ?</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Send email') }}</button>
        {{ form_end(form) }}
    </div
{% endblock %}