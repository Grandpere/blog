{% extends 'base.html.twig' %}

{% block title %}
    Liste des articles de la catégorie {{ tag.title }}
    {{ parent() }}
{% endblock %}

{% block body %}
    <h2>{{ tag.title }} ({{ articles|length }} article(s))</h2>
    {% for article in articles %}
        {% include 'web/article/_article_template.html.twig' %}
    {% else %}
        No records found
    {% endfor %}
    {% include '_pagination.html.twig' %}
{% endblock %}
